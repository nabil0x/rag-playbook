# Project 16 — Build Without LangChain

> **Goal:** Replace every block with raw libraries — no framework.

## Stack

```
requests + BeautifulSoup
SentenceTransformer
FAISS
OpenAI SDK
```

You already started this in `src/loaders/web.py` (plain `requests` + `bs4`).

## Article

- [ ] `01-rag-without-langchain.md`

## Code

Reference implementation lives under `scratch/` (pure Python, no framework imports)

## Notebook

`NoteBooks/Project-16-Build-Without-LangChain/01-rag-without-langchain.ipynb`
